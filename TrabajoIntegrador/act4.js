const prompt = require('prompt-sync')();
//Ejercicio 4: Calcular descuento por cantidad. Solicita al usuario la cantidad de productos comprados y el precio unitario. Usa una función flecha para calcular el total con descuento según la cantidad.
let cantidadProductos = parseInt(prompt('Ingrese la cantidad de productos: '));
let precioUnitario = parseFloat (prompt('Ingrese el valor unitario'));

let 